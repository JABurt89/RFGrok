Here are direct instructions for an AI coder to fix the "Error Invalid Workout Setup" toast when clicking "Skip Extra Set":

1. **Locate the Error Source**  
   - In `client/src/components/workout-logger.tsx`, find where the "Error Invalid Workout Setup" toast is triggered.

2. **Update STS Progression Logic**  
   - In `shared/progression.ts`, modify `STSProgression.calculate1RM` to handle `extraSetReps` as undefined or 0:
     ```typescript
     calculate1RM(sets: ExerciseSet[], extraSetReps?: number): number {
       if (sets.length === 0) return 0;
       const lastSet = sets[sets.length - 1];
       const W = lastSet.weight;
       const R = lastSet.reps;
       const S = sets.length;
       const C = Math.round(W * (1 + 0.025 * R) * (1 + 0.025 * (S - 1)) * 100) / 100;
       if (extraSetReps === undefined || extraSetReps === 0) return C;
       const F_full = Math.round(W * (1 + 0.025 * R) * (1 + 0.025 * S) * 100) / 100;
       const interpolated = C + (extraSetReps / R) * (F_full - C);
       return Math.round(interpolated * 100) / 100;
     }
     ```

3. **Update Workout Logger**  
   - In `client/src/components/workout-logger.tsx`, when "Skip Extra Set" is clicked:
     - Set `extraSetReps` to 0 in the workout log data.
     - Ensure 1RM is calculated with the standard formula (no extra set).

4. **Handle Edge Cases**  
   - In `client/src/components/workout-logger.tsx`, add checks:
     - If no sets are completed, skip 1RM calculation and log without error.
     - If extra set is skipped, proceed with standard 1RM calculation.

5. **Validate Data**  
   - Before saving in `client/src/components/workout-logger.tsx`, validate against `insertWorkoutLogSchema` from `shared/schema.ts`:
     ```typescript
     const validatedData = insertWorkoutLogSchema.parse(workoutLogData);
     ```

6. **Debug and Test**  
   - Add console logs in `client/src/components/workout-logger.tsx`:
     ```typescript
     console.log("Skipping extra set, extraSetReps:", extraSetReps);
     console.log("Calculated 1RM:", calculated1RM);
     ```
   - Test clicking "Skip Extra Set" to ensure no error toast appears.

7. **Review API Calls**  
   - In `client/src/components/workout-logger.tsx`, ensure the API call to save the workout log uses validated data and check server response for errors.

Implement these changes, then test thoroughly.